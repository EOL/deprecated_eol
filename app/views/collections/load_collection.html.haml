.create
  = form_tag({:action=>'submit_taxa_list'},:method=>'get') do
    = I18n.t("collection_loading.collection_name")
    = text_field_tag :collection_name, params[:collection_name]
    = I18n.t("collection_loading.add_list")
    = text_area_tag :list, params[:list], :cols => "40  ", :rows => "20"
    = submit_tag I18n.t("collection_loading.submit")