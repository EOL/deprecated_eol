<h1>Listing random_images</h1>

<table>
  <tr>
  </tr>

<% for random_image in @random_images %>
  <tr>
    <td><%= link_to 'Show', random_image %></td>
    <td><%= link_to 'Edit', edit_random_image_path(random_image) %></td>
    <td><%= link_to 'Destroy', random_image, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New random_image', new_random_image_path %>
