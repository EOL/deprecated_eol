%div{ :class => "description #{data_object.id}" }
  - if (data_object['description'])
    .credit.description
      .credit-caption
        = I18n.t(:description) 
      .credit-value
        = data_object['description']
      .footer
