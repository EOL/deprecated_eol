diff --git a/app/controllers/taxa/resources_controller.rb b/app/controllers/taxa/resources_controller.rb
index d66d6ee..5953609 100644
--- a/app/controllers/taxa/resources_controller.rb
+++ b/app/controllers/taxa/resources_controller.rb
@@ -88,8 +88,8 @@ private
     @news_and_event_links_contents ||= get_link_text([:news, :blog])
     @related_organizations_contents ||= get_link_text(:organization)
     @multimedia_links_contents ||= get_link_text([:multimedia, :paper])
-    @citizen_science_contents = get_toc_text([:citizen_science, :citizen_science_links])
-    @identification_contents = get_toc_text(:identification_resources)
+    @citizen_science_contents = get_toc_text([:citizen_science, :citizen_science_links ])
+    @identification_contents = get_toc_text([:identification_resources])
     @education_contents = get_toc_text(:education_for_resources_tab)
   end
 
@@ -102,13 +102,10 @@ private
   end
 
   def get_toc_text(which)
+    debugger
     # A little messier, since it can get arrays back:
     types = Array(which).flat_map { |t| TocItem.send(t) }.map(&:id)
-    @taxon_page.text(
-      language_ids: [ current_language.id ],
-      toc_ids: types,
-      filter_by_subtype: true
-    )
+    @taxon_page.text(  language_ids: [ current_language.id ],    toc_ids: types,filter_by_subtype: true    )
   end
 
   def show_add_link_buttons
diff --git a/app/models/taxon_concept.rb b/app/models/taxon_concept.rb
index 4732e67..2627077 100644
--- a/app/models/taxon_concept.rb
+++ b/app/models/taxon_concept.rb
@@ -762,6 +762,7 @@ class TaxonConcept < ActiveRecord::Base
 
   # TODO - this belongs in the same class as #overview_text_for_user. ...But be aware this is also used in TaxonDetails.
   def text_for_user(the_user = nil, options = {})
+    # debugger
     the_user ||= EOL::AnonymousUser.new(Language.default)
     options[:per_page] ||= 500
     options[:data_type_ids] = DataType.text_type_ids
@@ -772,6 +773,7 @@ class TaxonConcept < ActiveRecord::Base
   end
 
   def data_objects_from_solr(solr_query_parameters = {})
+    # debugger
     EOL::Solr::DataObjects.search_with_pagination(id, TaxonConcept.default_solr_query_parameters(solr_query_parameters))
   end
 
diff --git a/lib/eol/solr/data_objects.rb b/lib/eol/solr/data_objects.rb
index ed25815..96c090a 100644
--- a/lib/eol/solr/data_objects.rb
+++ b/lib/eol/solr/data_objects.rb
@@ -6,8 +6,8 @@ module EOL
         options[:page]        ||= 1
         options[:per_page]    ||= 30
         options[:per_page]      = 30 if options[:per_page] == 0
-
         response = solr_search(taxon_concept_id, options)
+        debugger
         total_results = response['response']['numFound']
         total_results += 1 if !(options[:exemplar_id].blank?) #increase total results count to add exemplar image
         results = response['response']['docs']
