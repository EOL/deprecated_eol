- inside_layout 'main' do
  #content
    #page-separator-general
    #page-title
      %h1
        = @page_title || 'Curator Central'
      %hr
    #page-content.clearfix
      #page-content-bgfauxinside
        #page-content.clearfix
          #full-page-content
            #left-pane
              = render(:partial=>'/curators/navigation')
            #right-pane
              = yield
