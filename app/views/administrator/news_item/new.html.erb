<%= render :partial => '/admin/content_top', :locals => {:name => 'New News Item'} %>
		   		    	
	<% form_for :news_item,@news_item,:url => {:action=>'create'},:html=>{:id=>'page_form'} do |f|  %>
	
	<%= render :partial => 'form', :locals => {:f=>f} %>
	
	<% end %>

<%= render :partial => '/admin/content_bottom' %>
