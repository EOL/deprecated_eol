#media_summary.gallery
  %h3= I18n.t(:media)
  - # TODO: Accessible JS for image gallery
  %ul.thumbnails
    - @media[0..3].each do |item|
      = render :partial => "media_thumbnail", :locals => {:item => item}
  %p.all= link_to I18n.t(:see_all_media_link), "/placeholder"
