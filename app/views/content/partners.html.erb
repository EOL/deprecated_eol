<div id="content">
	<div id="page-separator-general"></div>

    <!-- page title -->
    <div id="page-title">
          <h1><%= "Content Partners"[]%></h1>
        <hr />
    </div>
    <!-- /page title -->
	
	    <div id="page-content" class="clearfix">
	        <!-- center page content -->
	        <div id="full-page-content">
	        	
				<div id="partners">

					<% if @partners.nil? == false && @partners.size > 0 %>
					<p>Encyclopedia of Life content partners serve authenticated species information through the EOL portal. After developing a joint commitment, the content partners work closely with the EOL component groups to make their data visible within EOL.</p>
				<br />
					<p>Note that all of these partners maintain their own Web sites where you will find additional information not available through EOL. We urge you to click through to their sites to examine the richness of information each of them contains.</p>
						<br />
					    <table  width="100%" class="results_table" cellspacing="0">
					    <tr><th>Partner</th><th>Description</th></tr>
						<% for partner in @partners
							if partner.ready_for_agreement? 
								column_class=cycle('odd','even')  %>
						    	<tr>
						    	  <td width="25%" class="<%= column_class %>"><strong><%=eol_return_linked_text(partner.display_name,partner.homepage,:show_link_icon=>true)%></strong>
						    	    <br /><%= agent_logo(partner,'large') if !partner.logo_cache_url.blank?%></td>
						    	  <td class="<%= column_class %>"><%=partner.project_description%>
						    	    <%if partner.show_mou_on_partner_page? %> <%=link_to("See #{partner.display_name} agreement",{:controller=>'content_partner',:action=>'agreement',:id=>partner.id},{:target=>'_blank'})%>.<% end %>
						    	    </td>
						    	</tr>
							<% end
						 end %>
					    </table>

						<br />
						<form id="pagination_form">
						  <%= will_paginate @partners %>
            </form>
				    <% elsif @partners.nil? == false && @partners.size == 0 %>
						No content partners are available.
					<% end %>
	
			
			</div>
	    </div>
	
</div>

