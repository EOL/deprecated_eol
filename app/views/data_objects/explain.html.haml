#main
  %div.media
    - markdown = Redcarpet::Markdown.new(Redcarpet::Render::HTML)
    - @concepts.each do |concept|
      %h1= "#{concept.title.html_safe}"
        = markdown.render(concept.explain_entries).html_safe
