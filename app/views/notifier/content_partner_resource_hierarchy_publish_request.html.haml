= t('.message', :user_full_name => @user.full_name,
  :user_email => @user.email,
  :user_profile_url => user_url(@user, :host => $SITE_DOMAIN_OR_IP),
  :partner_full_name => @partner.full_name,
  :partner_resources_url => content_partner_resources_url(@partner, :host => $SITE_DOMAIN_OR_IP),
  :resource_title => @resource.title,
  :resource_url => content_partner_resource_url(@partner, @resource, :host => $SITE_DOMAIN_OR_IP),
  :hierarchy_edit_url => edit_content_partner_resource_hierarchy_url(@partner, @resource, @hierarchy, :host => $SITE_DOMAIN_OR_IP))