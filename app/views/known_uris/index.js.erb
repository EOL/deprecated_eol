$('div#suggestions .children').hide();
$("#category_<%= @category.id %> .children").html('<%= escape_javascript render(:partial => 'index') %>').show();
$('li.attribute').click(function() {
  $('#user_added_data_predicate').val($(this).find('.name').text());
  EOL.attribute_is_okay();
});
