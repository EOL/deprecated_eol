- curators = @taxon_concept.acting_curators
#page-citation
  %a{:name => 'citation'}
  - if (curators.size > 0)
    = curators.map{|c| link_to h("#{c.given_name} #{c.family_name}"), :controller => :account, :action => :show, :id => c.id, :popup => true}.join(', ') + "."
    - if curators.size == 1
      = "Editor."
    - elsif curators.size > 1
      = "Editors."
  =I18n.t("_var__taxon_concept_title__ses", :var__taxon_concept_title__session_hierarchy_ => @taxon_concept.title(@session_hierarchy))
  %a{ :href => this_url = url_for(:controller=>'/taxa', :action=>'show', :id=>@taxon_concept.id), :target => "_blank" }><
    =I18n.t("http___var__site_domain_or_ipv", :var__site_domain_or_ip => $SITE_DOMAIN_OR_IP, :var_this_url => this_url)
  =I18n.t("___accessed")
  == #{Time.now.strftime("%d %b %Y")}.

  /
    =I18n.t("example_")
    =I18n.t("fisher__brian___pogonomyrmex_a")
