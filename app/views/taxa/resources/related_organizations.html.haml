#taxon_resources
  = render 'menu'
  #taxon_detail
    #main.main_container
      %h3= I18n.t(:related_organizations)
      .section
        - unless @related_organizations_contents.blank?
          = render 'link_objects', { content: @related_organizations_contents }
