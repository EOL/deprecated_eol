All user-submitted texts on the website are shown below, sorted inversely by time created.
Click on a user to view the user's detailed profile, click on the species to view the page where the text exists (use your browser back button to return here).
#comments
  - form_tag({:action=>'index'},:method=>'get') do
    Select:
    %select{ :name => "user_id" }
      %option
        All
      - for user in @user_list
        %option{ :value => user.id, :selected => @user_id.to_i == user.id }
          = h(user.family_name)
          = ", "
          = h(user.given_name)
    = submit_tag 'Search'
    %br
    %br
    - if @users_data_objects.nil? == false && @users_data_objects.length > 0
      User-submitted text found:
      = @users_data_objects.total_entries
      %br
      = will_paginate @users_data_objects
      %table.results_table{ :cellspacing => "0" }
        %tr
          %th
            User
          %th
            Species
          %th
            Description
          %th
            TOC
          %th
            Vetted
            %br
            Visible
            %br
            Published
          %th
            Created
            %br
            Updated
        - for udo in @users_data_objects
          - row_class = cycle('odd','even')
          - if udo.taxon_concept_id
            - concept = TaxonConcept.find(udo.taxon_concept_id)
            - name = concept.quick_scientific_name(:italicized)
          - else
            - name = ""
          %tr{ :valign => "top" }
            %td{ :class => row_class }
              = link_to udo.user.full_name, :controller => '/administrator/user', :action => 'edit' , :id => udo.user_id
            %td{ :class => row_class }
              = link_to name, :controller => '/taxa', :action => :show, :id => udo.taxon_concept_id
            %td{ :class => row_class }
              = link_to "Permalink", :controller => '/data_objects', :action => :show, :id => udo.data_object_id
              %br
              = h truncate(udo.data_object.description, 500)
            %td{ :class => row_class }
              = udo.data_object.toc_items[0].label
            %td{ :class => row_class }
              = udo.data_object.vetted.label
              %br
              = udo.data_object.visibility.label
              %br
              = h udo.data_object.published
            %td{ :class => row_class }
              = h format_date_time(udo.data_object.created_at, :format=>'short_no_tz')
              %br
              = h format_date_time(udo.data_object.updated_at, :format=>'short_no_tz')
      %br
      = will_paginate @users_data_objects
    - else
      No user-submitted data objects match your search criteria.
