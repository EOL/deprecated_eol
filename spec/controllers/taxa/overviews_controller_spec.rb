require 'spec_helper'

describe Taxa::OverviewsController do

  #Delete this example and add some real ones
  it "should use Taxa::OverviewsController" do
    controller.should be_an_instance_of(Taxa::OverviewsController)
  end

end
