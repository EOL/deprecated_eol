= t('.message', :user_full_name => @user.full_name,
  :user_email => @user.email,
  :user_profile_url => user_url(@user, :host => $IP_ADDRESS_OF_SERVER),
  :partner_full_name => @partner.full_name,
  :partner_resources_url => content_partner_resources_url(@partner, :host => $IP_ADDRESS_OF_SERVER),
  :resource_title => @resource.title,
  :resource_url => content_partner_resource_url(@partner, @resource, :host => $IP_ADDRESS_OF_SERVER),
  :resource_updated_at_date => @resource.updated_at,
  :resource_status => @resource.status_label)