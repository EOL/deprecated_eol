= t('.message', :user_full_name => @user.full_name,
  :user_email => @user.email,
  :user_profile_url => user_url(@user, :host => EOL::Server.domain),
  :partner_full_name => @partner.full_name,
  :partner_profile_url => content_partner_url(@partner, :host => EOL::Server.domain),
  :partner_created_at_date => @partner.created_at)
