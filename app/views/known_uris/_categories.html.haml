%h3= I18n.t(:categories_title)
= I18n.t(:category_suggestions)
%ul
  -# TODO - I don't think we want #selectable_toc, but I'm not sure what we do want.
  - TocItem.selectable_toc.select { |r| ! r.label.blank? }.sort_by(&:label).each do |category|
    %li= link_to category.label, known_uris_path(category_id: category.id), :remote => true
