- expand ||= false
%div#hierarchy_browser
  %ul#hierarchy_browser_list.branch
    - unless @hierarchy_entry.nil?
      %b
        =I18n.t("browsing_branch_colon")
      - ancestors = @hierarchy_entry.ancestors
      - hierarchy_entry = ancestors.pop
      = render('navigation/node_with_ancestry', { :expand => expand, :ancestors => ancestors, :hierarchy_entry => hierarchy_entry })
    %br
    %b
      =I18n.t("hierarchy_roots_colon")
    - for kingdom in @hierarchy_entry.hierarchy.kingdoms
      %li.node
        = render('navigation/node_name', { :hierarchy_entry => kingdom })
