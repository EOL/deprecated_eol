- if @posts.blank?
  .empty
    %p= I18n.t('forums.topics.no_posts')
- else
  = will_paginate @posts, :renderer => ForumTopicLinkRenderer
  - @posts.each do |post|
    = render 'forums/posts/show', { :post => post }
  = will_paginate @posts, :renderer => ForumTopicLinkRenderer
= render 'forums/posts/form'
