require File.dirname(__FILE__) + '/../spec_helper'

describe PageName do

  # before(:each) do
  #   Scenario.load :foundation
  # end  
  # 
  # it "should have page names" do
  #   tc = build_taxon_concept(:toc=> [
  #     {:toc_item => TocItem.bhl}
  #   ])
  #   PageName.gen(:name_id => tc.id)
  #   PageName.page_names_for?(tc.id).should == true
  # end
  # 
  # it "should have page names" do
  #   tc = build_taxon_concept(:toc=> [
  #     {:toc_item => TocItem.bhl}
  #   ])
  #   
  #   PageName.destroy_all # Destroy the pages forcing the SQL to return an empty set
  #   PageName.page_names_for?(tc.id).should == false
  # end

end