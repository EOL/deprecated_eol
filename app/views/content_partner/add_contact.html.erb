<%= render :partial=> 'top', :locals=>{:page_header => @page_header} %>

	        This information will allow us to make contact with you.  It will not be visible on the EOL web site. You must have at least one contact.
                    <%= eol_lang_error_messages_for :agent_contact, :header_message => "Contact Errors"[] %>

                    <!-- begin form -->
                    <% form_for @agent_contact, :url => { :action => 'add_contact' }, :html => { :method => :post, :id => 'contact_form', :class => "warning-warn invalid-invalid styleLabelOnErr" } do |f| %>
                        <%= render :partial => 'contact_form', :locals => { :f => f }%>                        
                    <% end -%>
                    <!-- end form -->
                                        
                    <br /><br />
                    <div class="button">                        
                        <%= content_partner_submit_buttons :id => 'contact_form' %>
                    </div>

<%= render :partial=> 'bottom' %>
