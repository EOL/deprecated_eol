require File.dirname(__FILE__) + '/../spec_helper'

describe EOL::OpenAuth do

  # TODO: Re-write these tests

  # before(:all) do
  #   OAUTH_CONFIG = YAML.load_file("#{RAILS_ROOT}/config/oauth.yml")[RAILS_ENV]
  #   @twitter_config = OAUTH_CONFIG["twitter"]
  #   @facebook_config = OAUTH_CONFIG["facebook"]
  #   
  # end
  # 
  # it '#consumer should return OAuth::Consumer' do
  #   client = EOL::Oauth.consumer(@twitter_config)
  #   client.class.should == OAuth::Consumer
  # end
  # 
  # it '#consumer2 should return OAuth2::Client' do
  #   client = EOL::Oauth.consumer2(@facebook_config)
  #   client.class.should == OAuth2::Client
  # end

end