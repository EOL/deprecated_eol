#classification_tree.article
  - count = @taxon_page.hierarchy_entries.length
  .header
    %h3= I18n.t(:classification_tree_header)
    - if count > 1
      = link_to(raw(I18n.t(:see_all_classifications_with_count_html, :count => count)), taxon_names_path(@taxon_page))
  #classification_browser.browsable.classifications
    = render :partial => 'navigation/root_nodes'
